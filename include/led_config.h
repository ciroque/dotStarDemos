#pragma once

// Easy hardware settings for class setup.
// Change these values if your strip length or pins are different.
constexpr int NUM_PIXELS = 60;
constexpr int DATA_PIN = 4;
constexpr int CLOCK_PIN = 5;
